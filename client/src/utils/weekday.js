/*
javascript date object has a method called getDay which returns
a nuber between 0 - 6 representing the day of the week.
I can also use the weekday.IndexOf method to get the 0 based index value of the day or the week if I want to convert from a string day to the number
*/

const  weekday = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];

module.exports = {weekday};